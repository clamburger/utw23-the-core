export enum DisplayState {
  Connecting,
  Disconnected,
  Ready,
  FirstTimeSetup,
  TeamDashboard,
  AdminDashboard,
  RegisteringCards,
  ResettingCards
}

export enum CardType {
  Admin,
  Team,
  Credits,
  Reward,
  Special
}